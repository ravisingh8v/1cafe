import { createStore } from 'vuex'
import getters from './auth/getters'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    getters
  }
})
